<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.company.MatchFootball.mapper.DoeunMapper"> <!-- 패키지.EmpMapper -->
<!-- 프로필 조회  -->
<select id="getUser" parameterType="MembersVO" resultType="MembersVO">
select * from members
where id = #{id}
</select>

<!-- 프로필 입력 -->


<!-- 프로필 조회  -->
 <select id="MemInfo" parameterType="MembersVO">
update members set
gender = #{gender},
location1= #{location1},
location2= #{location2},
pnum = #{pnum},
birth = ${birth},
img = #{img},
pos = ${pos}
where id = #{id}


</select>

<insert id="insertMem" parameterType="MembersVO">
    insert into members(
      email,
      name,
      gender,

      img,
      pos,
      pnum,
      lv,
      postCode,
      location1,
      location2,
      birth
      )
      
    values(
      #{email},
      #{name},
      #{gender},

      #{img},
      #{pos},
      #{pnum},
      #{level},
      #{postCode},
      #{address},
      #{location2},
      #{birth})
  </insert>

<update id="upMem" parameterType="MembersVO" >
		
	update members 
	set 
	name=#{name}, 
	gender=#{gender}, 
	postcode=#{postCode},
	location1=#{location1}, 
	location2=#{location2}, 
	pnum=#{pnum}, 
	email=#{email}, 
	birth=#{birth}, 
	img=#{img}, 
	lv= #{lv}, 
	pos=#{pos} 
  where id = #{id}

</update>

<!-- 총 포인트 확인 -->
<select id="selTpo" parameterType="PointVO">
select sum(p.npoint) from point p where p.p_id = #{p_id};
</select>
</mapper>
